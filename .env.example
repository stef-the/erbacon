# No environment variables are required to run or deploy this site.
#
# Which Google Sheet tab feeds which page is configured in code:
#   src/lib/sheets.ts
#
# Just `pnpm install` and `pnpm run dev`. The sheet must remain shared as
# "Anyone with the link → Viewer" so the site can read it.
